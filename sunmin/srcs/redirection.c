#include "minishell.h"

char	**put_redirection_file(t_line **line)
{
	char	**file;

	return (file);
}

int		ft_redirection(t_line **line)
{
	char	**file;	// 리다이렉션 이름
	int		*type;	// 리다이렉션 구분자

	file = put_redirection_file(line);
}
